<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>
<h3><%= @user.name.capitalize %>'s Events</h3>
<% @events_made_by_current_user.each do  |e| %>
<ul>
<li><%= e.title%></li>
</ul>
<% end %>

<% if current_user && current_user.name ==  @user.name %>
<%= link_to 'Edit', edit_user_path(@user) %> |
<% end %>
<%= link_to 'Back', users_path %>
